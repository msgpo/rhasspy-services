#!/usr/bin/env bash
set -e

temp_list="$(mktemp)"
function finish {
    rm -f "${temp_list}"
}

trap finish EXIT

# -----------------------------------------------------------------------------

cat > "${temp_list}"
phonetisaurus-apply --word_list "${temp_list}" "$@" | \
    python3 -m training.vocab_g2p
